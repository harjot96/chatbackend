-- Initial database schema
-- This migration creates all the initial tables

-- Note: This is a placeholder migration
-- The actual schema is created by database/init.js
-- Future schema changes should be added as new migration files

-- Example: Add a column (for future migrations)
-- ALTER TABLE users ADD COLUMN IF NOT EXISTS phone VARCHAR(20);

SELECT 'Initial schema already created by init.js' as message;

